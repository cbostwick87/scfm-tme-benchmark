# DECISIONS log

Every deviation from the pre-specified analysis plan, every fallback taken, and every
judgement call with a scientific consequence, recorded with reason and timestamp
(UTC, ISO-8601). Required by provenance requirement P7. This log is surfaced in the
report, not buried.

Append-only. Newest entries at the bottom. Entries are written when the decision is
made, not reconstructed afterwards.

| # | timestamp (UTC) | phase | decision | reason | consequence |
|---|---|---|---|---|---|
| 1 | 2026-07-31T17:30Z | 0 setup | Author field set to a personal address rather than the GitHub `noreply` form. | Operator instruction, asked and answered explicitly. | The address is permanently public in commit history and is routinely scraped. Flagged to the operator before the first commit. |
| 2 | 2026-07-31T17:41Z | 0 setup | Foundation-model packages (Geneformer, scGPT) moved out of the core conda solve and installed separately. | Geneformer is distributed from its own repository rather than PyPI, and scGPT depends on flash-attn which is not expected to build on a Turing-class GPU. A failure in either must not be able to break the environment every baseline and statistic depends on. | Core environment solved cleanly in ~4 min. Model-package installation is isolated and its fallback is recorded separately. |
| 3 | 2026-07-31T17:44Z | 0 setup | fp16 confirmed as the only accelerated reduced precision on this GPU; bf16 explicitly forbidden in config despite the runtime reporting it as supported. | torch.cuda.is_bf16_supported() returns True on this Turing card and a bf16 matmul completes without error, so a naive capability check would silently select it. Measured throughput: fp16 26.3 TFLOP/s, fp32 4.42, bf16 2.45 -- bf16 is emulated, 10.7x slower than fp16 and slower even than fp32. | All embedding extraction pinned to fp16 autocast. A bf16 path would not have failed loudly; it would merely have made the GPU phase ~10x slower with no error. |
| 4 | 2026-07-31T17:52Z | 0 setup | transformers pinned to 4.44.2, downgraded from the 5.14.1 that the unpinned solve selected. | transformers 5.x requires torch>=2.4 and, finding torch 2.3.1, DISABLED its PyTorch backend with only a stderr notice -- imports still succeeded but no model could be instantiated. Geneformer embedding extraction would have failed in Phase D. torch was held at 2.3.1 because the scvi-tools/jax pin set is validated against it. | Verified by a real model instantiation plus an fp16 autocast forward pass on GPU, not by an import check. |
| 5 | 2026-07-31T18:05Z | 1 data | Duplicate accession GSE143423 resolved: BRCA_GSE143423 dropped, NSCLC_GSE143423 retained. | Both TISCH2 entries are the same submission (brain metastasis, Source=mBrain, Stage=Metastatic) with zero shared cell barcodes but non-independent origin. BRCA_GSE143423 independently fails all three selection criteria: 1 donor, 1 mappable immune class, 241 immune cells. | Removes the S3 leakage path for this accession. The retained entry contributes 3 donors and 2,236 immune cells. |
| 6 | 2026-07-31T18:05Z | 1 data | Duplicate accession GSE150660 resolved: NSCLC_GSE150660 dropped, BRCA_GSE150660 retained. | One submission (GSM4555887-91) split across two TISCH2 cancer-type entries with disjoint patients (A,D vs B,C,E) and zero shared barcodes -- genuinely disjoint sub-cohorts of one study, so they are not independent for leave-dataset-out. NSCLC_GSE150660 has only 2 donors and cannot form a three-way donor split under S2. | The retained entry contributes 3 donors and 8,762 immune cells. Had both been kept as separate datasets, S3 would have placed one study's cells on both sides of the split. |
| 7 | 2026-07-31T18:20Z | 1 data | Corpus expanded beyond the pre-downloaded 14 TISCH2 datasets by fetching additional accessions, screened on metainfo tables before any expression matrix was downloaded. | Applying the selection criteria to the pre-downloaded corpus left LIHC represented by only 693 and 1,547 immune cells, too thin to support a label-budget sweep to 100 cells/class. Operator asked whether more data could satisfy the criteria more completely rather than accepting the trade-off. | LIHC_GSE140228_10X (5 donors, 10 immune classes, 60,468 immune cells) and further datasets now qualify. Only ~0.1 GiB of TSV was transferred to screen candidates, deferring multi-GiB matrix downloads to selected datasets only. |
| 8 | 2026-07-31T18:22Z | 1 data | Two further duplicate-accession hazards identified in the expanded candidate pool and pre-emptively grouped: GSE125449 (LIHC and CHOL entries) and GSE140228 (10X and Smart-seq2 entries). | Screening the candidate pool for shared GSE accessions before selection, rather than after, surfaced the same hazard class the brief flagged for the original corpus. GSE140228 10X and Smart-seq2 are the same study measured on two assays; GSE125449 spans two cancer types. | Each accession is one leave-dataset-out group regardless of how many TISCH2 entries it produces. Grouping is enforced by the `group` column, which the S3 splitter uses as its holdout unit. |
| 9 | 2026-07-31T19:20Z | 0 setup | Environment lockfiles sanitised before commit: internal package-index host lines removed and absolute environment paths replaced with placeholders. | The automatically generated `pip freeze` and `conda env export` output embedded an internal package-index address and absolute host paths. A pre-commit scan for internal identifiers caught them; they would otherwise have entered a public repository's permanent history. | Package names and versions are unmodified, so the lockfiles remain functional for reproduction. The scan runs before every push, not only at project end. |
| 10 | 2026-07-31T21:10Z | 1 data | Census queries rewritten to use pre-aggregated summary tables and narrow server-side filters; materialising the full obs frame is now forbidden. | A Census query in this phase read every non-normal-disease human cell (34,126,943 rows x 6 columns) into a pandas DataFrame and then cast the columns to str. On a 15 GiB host that is an unrecoverable allocation, and the box thrashed into an unresponsive state requiring a forced restart. The brief warned that host RAM, not VRAM, is the binding constraint; this was a design error of exactly the kind it warned about. | No analysis output was lost -- the repository was pushed and verified at 52a41b5 before the stall, which is precisely why the push-at-every-boundary rule exists. Rule adopted: never call obs.read() without a restrictive value_filter, and prefer census_info/summary_cell_counts for anything that only needs counts. |
| 11 | 2026-07-31T21:12Z | 0 setup | earlyoom installed on the host by the operator (-m 10 -s 20 -r 3600), preferring Python/R processes as kill targets. | Swap on this host is EBS-backed, so swap exhaustion is the thrash signal; watching available memory alone detects the problem only after the box is already unrecoverable. Monitoring both thresholds catches it earlier. | A job that exceeds the memory budget is now SIGKILLed rather than allowed to degrade the whole box. A killed job leaves no Python-side traceback, so `journalctl -u earlyoom` is the first diagnostic for any job that dies without one. Recorded in the host provenance notes. |
| 12 | 2026-07-31T21:14Z | 1 data | Raw-file integrity gate strengthened: an expression file counts as staged only if h5py can open it and read its keys, not merely if the file exists. | The interrupted staging run left a partially copied 1.3 GiB HDF5 file that existed at a plausible size. A file-exists or magic-byte check would have accepted it and any resulting QC failure would have surfaced much later, far from its cause. | One truncated file was detected and re-staged. All staging is idempotent and resumable, so an interrupted run costs only the unfinished transfers. |
| 13 | 2026-07-31T21:35Z | 1 data | Census version pinned to 2025-11-08 and recorded (P2). Custom TileDB context must extend the package defaults, not replace them. | Building a bare SOMATileDBContext to cap memory dropped vfs.s3.no_sign_request, and the public Census bucket returned HTTP 403 AccessDenied. Starting from cellxgene_census's DEFAULT_TILEDB_CONFIGURATION and only tightening soma.init_buffer_bytes (64 MiB) and sm.mem.total_budget (2 GiB) works. | Census reads now run inside a 9 GiB address-space cap, so an over-large query fails the job rather than the host. |
| 14 | 2026-07-31T21:40Z | 1 data | First pretraining-overlap test (GEO accession search of Census metadata) DISCARDED as invalid; audit redone on DOI and citation fields. | The accession search returned zero hits for all 13 datasets, which looked like a clean negative result. Control probes for accessions known to be in Census also returned zero, proving the test could not detect a positive: Census metadata does not carry GEO accession strings. | The invalid test would have reported 'no overlap anywhere' -- a comfortable and wrong conclusion on the study's most important confound. The corrected DOI-based audit found a real overlap that the accession search had missed entirely. |
| 15 | 2026-07-31T21:41Z | 1 data | BRCA_GSE176078 flagged as suspected pretraining overlap and RETAINED, with a pre-specified sensitivity analysis excluding it. | Wu et al. 2021 (10.1038/s41588-021-00911-1) is present in CELLxGENE Census 2025-11-08 as 13 datasets / 225,725 cells including T-cell, B-cell, myeloid and plasmablast compartments. scGPT's corpus was drawn from CELLxGENE, so these cells may have been seen in pretraining. Dropping it would bias the corpus toward datasets whose overlap merely could not be DETECTED, which is worse than measuring the effect. | Every primary contrast (H1, H2) will be recomputed with this dataset excluded, specified before any result is known so it cannot become a post-hoc choice. If the conclusion changes, that is the headline caveat, not a footnote. |
| 16 | 2026-07-31T21:52Z | 2 harmonise | Author label vocabulary audited against the actual corpus before running: 29 unique labels found, 8 of which had no rule. All 29 now have an explicit rule. | labels.harmonise_labels raises on any label without a mapping or drop rule, so the mapping had to be verified complete rather than assumed. The audit found 'Tproilf' -- a misspelling of 'Tprolif' in one dataset's source metainfo (330 cells). | The typo was given its OWN explicit drop rule rather than being normalised by fuzzy matching: a misspelling in upstream data is a fact about the data, and silently correcting it would hide that the vocabulary is unclean. 14 working classes (10 immune + 4 context), 57,926 cells (6.8%) dropped with a stated reason each. |
| 17 | 2026-07-31T21:53Z | 2 harmonise | ILC (innate lymphoid cells) dropped rather than merged into NK. | ILC are genuinely immune but appear in a single dataset at 202 cells corpus-wide. They cannot support a class in a cross-dataset design, and folding them into NK would assert a lineage equivalence the data does not establish. | Dropped and counted in T4 rather than merged. The count is reported so the choice is visible. |
| 18 | 2026-07-31T21:58Z | 2 harmonise | Accounting tables (qc_summary, T4) rebuilt from per-dataset sidecars instead of the rows produced by the current invocation. | REAL BUG found by testing resumability: on a resumed run, skipped datasets contribute no rows, and the whole-file overwrite reduced a 13-row qc_summary to a 1-row file while the stage reported success. Resumability was silently destroying the accounting. | Sidecars are written per dataset at the moment it is processed, and the stage now REFUSES to write a partial accounting table. Verified by running --force then a resumed run: 13 QC rows and 126 T4 rows survive both. |
| 19 | 2026-07-31T21:56Z | 2 harmonise | Cached-artefact resume check strengthened: a processed h5ad is trusted only if it opens AND carries the required obs columns. | The crashed first run left a BRCA_GSE161529.h5ad that existed at a plausible size but could not be opened by h5py. The original existence check trusted it, which silently dropped that dataset from all accounting. | Unreadable or incomplete caches are now deleted and re-derived. This is the second time in this project that an existence check accepted a truncated file; integrity is verified by opening, never by presence or size. |
| 20 | 2026-07-31T21:59Z | 2 harmonise | QC thresholds removed ZERO cells; documented as an inert guard with measured evidence rather than presented as a filtering step. | Measured on the raw matrix: minimum genes/cell is exactly 500 (a hard upstream floor), 1st percentile 520, and pct_mito tops out at 10.8% against a 20% threshold. TISCH2 distributes already-QC'd matrices. Three datasets (CRC_GSE108989, HNSC_GSE103322, LIHC_GSE98638) contain no MT- genes at all, so the mito filter is structurally inapplicable there. | Thresholds retained as an assertion that inputs are pre-filtered, not as a claim that filtering occurred. Recorded per dataset as mito_filter_applied and written up in results/qc_notes.md so the report cannot imply uniform QC. |
| 21 | 2026-07-31T22:05Z | 2 harmonise | PRIMARY macro-F1 is computed over classes present in BOTH the train and test partition of a given contrast; macro-F1 over ALL test-partition classes is computed and reported alongside it as a secondary metric in T2 and T3. | Class coverage is uneven across the corpus (CD8_T in 13/13 datasets, Neutrophil 2, Treg 3, Epithelial 3, NK 4). Under S3 and often S2 a test partition contains classes absent from training, which no classifier can predict and which score F1=0. That penalty is identical for every representation, so it adds equal noise to every arm and dilutes the representation contrast the study exists to measure -- it reflects corpus composition, not representation quality. | The evaluation class set varies per contrast, so absolute macro-F1 is not directly comparable ACROSS split schemes; the class set and its size are recorded per row in T2 so every number is interpretable. Both metrics are reported, so a reader can see whether the choice changed any conclusion. Decision taken before any result exists. |
