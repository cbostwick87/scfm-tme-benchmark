# DECISIONS log

Every deviation from the pre-specified analysis plan, every fallback taken, and every
judgement call with a scientific consequence, recorded with reason and timestamp
(UTC, ISO-8601). Required by provenance requirement P7. This log is surfaced in the
report, not buried.

Append-only. Newest entries at the bottom. Entries are written when the decision is
made, not reconstructed afterwards.

| # | timestamp (UTC) | phase | decision | reason | consequence |
|---|---|---|---|---|---|
| 1 | 2026-07-31T17:30Z | 0 setup | Author field set to a personal address rather than the GitHub `noreply` form. | Operator instruction, asked and answered explicitly. | The address is permanently public in commit history and is routinely scraped. Flagged to the operator before the first commit. |
| 2 | 2026-07-31T17:41Z | 0 setup | Foundation-model packages (Geneformer, scGPT) moved out of the core conda solve and installed separately. | Geneformer is distributed from its own repository rather than PyPI, and scGPT depends on flash-attn which is not expected to build on a Turing-class GPU. A failure in either must not be able to break the environment every baseline and statistic depends on. | Core environment solved cleanly in ~4 min. Model-package installation is isolated and its fallback is recorded separately. |
| 3 | 2026-07-31T17:44Z | 0 setup | fp16 confirmed as the only accelerated reduced precision on this GPU; bf16 explicitly forbidden in config despite the runtime reporting it as supported. | torch.cuda.is_bf16_supported() returns True on this Turing card and a bf16 matmul completes without error, so a naive capability check would silently select it. Measured throughput: fp16 26.3 TFLOP/s, fp32 4.42, bf16 2.45 -- bf16 is emulated, 10.7x slower than fp16 and slower even than fp32. | All embedding extraction pinned to fp16 autocast. A bf16 path would not have failed loudly; it would merely have made the GPU phase ~10x slower with no error. |
| 4 | 2026-07-31T17:52Z | 0 setup | transformers pinned to 4.44.2, downgraded from the 5.14.1 that the unpinned solve selected. | transformers 5.x requires torch>=2.4 and, finding torch 2.3.1, DISABLED its PyTorch backend with only a stderr notice -- imports still succeeded but no model could be instantiated. Geneformer embedding extraction would have failed in Phase D. torch was held at 2.3.1 because the scvi-tools/jax pin set is validated against it. | Verified by a real model instantiation plus an fp16 autocast forward pass on GPU, not by an import check. |
| 5 | 2026-07-31T18:05Z | 1 data | Duplicate accession GSE143423 resolved: BRCA_GSE143423 dropped, NSCLC_GSE143423 retained. | Both TISCH2 entries are the same submission (brain metastasis, Source=mBrain, Stage=Metastatic) with zero shared cell barcodes but non-independent origin. BRCA_GSE143423 independently fails all three selection criteria: 1 donor, 1 mappable immune class, 241 immune cells. | Removes the S3 leakage path for this accession. The retained entry contributes 3 donors and 2,236 immune cells. |
| 6 | 2026-07-31T18:05Z | 1 data | Duplicate accession GSE150660 resolved: NSCLC_GSE150660 dropped, BRCA_GSE150660 retained. | One submission (GSM4555887-91) split across two TISCH2 cancer-type entries with disjoint patients (A,D vs B,C,E) and zero shared barcodes -- genuinely disjoint sub-cohorts of one study, so they are not independent for leave-dataset-out. NSCLC_GSE150660 has only 2 donors and cannot form a three-way donor split under S2. | The retained entry contributes 3 donors and 8,762 immune cells. Had both been kept as separate datasets, S3 would have placed one study's cells on both sides of the split. |
| 7 | 2026-07-31T18:20Z | 1 data | Corpus expanded beyond the pre-downloaded 14 TISCH2 datasets by fetching additional accessions, screened on metainfo tables before any expression matrix was downloaded. | Applying the selection criteria to the pre-downloaded corpus left LIHC represented by only 693 and 1,547 immune cells, too thin to support a label-budget sweep to 100 cells/class. Operator asked whether more data could satisfy the criteria more completely rather than accepting the trade-off. | LIHC_GSE140228_10X (5 donors, 10 immune classes, 60,468 immune cells) and further datasets now qualify. Only ~0.1 GiB of TSV was transferred to screen candidates, deferring multi-GiB matrix downloads to selected datasets only. |
| 8 | 2026-07-31T18:22Z | 1 data | Two further duplicate-accession hazards identified in the expanded candidate pool and pre-emptively grouped: GSE125449 (LIHC and CHOL entries) and GSE140228 (10X and Smart-seq2 entries). | Screening the candidate pool for shared GSE accessions before selection, rather than after, surfaced the same hazard class the brief flagged for the original corpus. GSE140228 10X and Smart-seq2 are the same study measured on two assays; GSE125449 spans two cancer types. | Each accession is one leave-dataset-out group regardless of how many TISCH2 entries it produces. Grouping is enforced by the `group` column, which the S3 splitter uses as its holdout unit. |
| 9 | 2026-07-31T19:20Z | 0 setup | Environment lockfiles sanitised before commit: internal package-index host lines removed and absolute environment paths replaced with placeholders. | The automatically generated `pip freeze` and `conda env export` output embedded an internal package-index address and absolute host paths. A pre-commit scan for internal identifiers caught them; they would otherwise have entered a public repository's permanent history. | Package names and versions are unmodified, so the lockfiles remain functional for reproduction. The scan runs before every push, not only at project end. |
| 10 | 2026-07-31T21:10Z | 1 data | Census queries rewritten to use pre-aggregated summary tables and narrow server-side filters; materialising the full obs frame is now forbidden. | A Census query in this phase read every non-normal-disease human cell (34,126,943 rows x 6 columns) into a pandas DataFrame and then cast the columns to str. On a 15 GiB host that is an unrecoverable allocation, and the box thrashed into an unresponsive state requiring a forced restart. The brief warned that host RAM, not VRAM, is the binding constraint; this was a design error of exactly the kind it warned about. | No analysis output was lost -- the repository was pushed and verified at 52a41b5 before the stall, which is precisely why the push-at-every-boundary rule exists. Rule adopted: never call obs.read() without a restrictive value_filter, and prefer census_info/summary_cell_counts for anything that only needs counts. |
| 11 | 2026-07-31T21:12Z | 0 setup | earlyoom installed on the host by the operator (-m 10 -s 20 -r 3600), preferring Python/R processes as kill targets. | Swap on this host is EBS-backed, so swap exhaustion is the thrash signal; watching available memory alone detects the problem only after the box is already unrecoverable. Monitoring both thresholds catches it earlier. | A job that exceeds the memory budget is now SIGKILLed rather than allowed to degrade the whole box. A killed job leaves no Python-side traceback, so `journalctl -u earlyoom` is the first diagnostic for any job that dies without one. Recorded in the host provenance notes. |
| 12 | 2026-07-31T21:14Z | 1 data | Raw-file integrity gate strengthened: an expression file counts as staged only if h5py can open it and read its keys, not merely if the file exists. | The interrupted staging run left a partially copied 1.3 GiB HDF5 file that existed at a plausible size. A file-exists or magic-byte check would have accepted it and any resulting QC failure would have surfaced much later, far from its cause. | One truncated file was detected and re-staged. All staging is idempotent and resumable, so an interrupted run costs only the unfinished transfers. |
| 13 | 2026-07-31T21:35Z | 1 data | Census version pinned to 2025-11-08 and recorded (P2). Custom TileDB context must extend the package defaults, not replace them. | Building a bare SOMATileDBContext to cap memory dropped vfs.s3.no_sign_request, and the public Census bucket returned HTTP 403 AccessDenied. Starting from cellxgene_census's DEFAULT_TILEDB_CONFIGURATION and only tightening soma.init_buffer_bytes (64 MiB) and sm.mem.total_budget (2 GiB) works. | Census reads now run inside a 9 GiB address-space cap, so an over-large query fails the job rather than the host. |
| 14 | 2026-07-31T21:40Z | 1 data | First pretraining-overlap test (GEO accession search of Census metadata) DISCARDED as invalid; audit redone on DOI and citation fields. | The accession search returned zero hits for all 13 datasets, which looked like a clean negative result. Control probes for accessions known to be in Census also returned zero, proving the test could not detect a positive: Census metadata does not carry GEO accession strings. | The invalid test would have reported 'no overlap anywhere' -- a comfortable and wrong conclusion on the study's most important confound. The corrected DOI-based audit found a real overlap that the accession search had missed entirely. |
| 15 | 2026-07-31T21:41Z | 1 data | BRCA_GSE176078 flagged as suspected pretraining overlap and RETAINED, with a pre-specified sensitivity analysis excluding it. | Wu et al. 2021 (10.1038/s41588-021-00911-1) is present in CELLxGENE Census 2025-11-08 as 13 datasets / 225,725 cells including T-cell, B-cell, myeloid and plasmablast compartments. scGPT's corpus was drawn from CELLxGENE, so these cells may have been seen in pretraining. Dropping it would bias the corpus toward datasets whose overlap merely could not be DETECTED, which is worse than measuring the effect. | Every primary contrast (H1, H2) will be recomputed with this dataset excluded, specified before any result is known so it cannot become a post-hoc choice. If the conclusion changes, that is the headline caveat, not a footnote. |
| 16 | 2026-07-31T21:52Z | 2 harmonise | Author label vocabulary audited against the actual corpus before running: 29 unique labels found, 8 of which had no rule. All 29 now have an explicit rule. | labels.harmonise_labels raises on any label without a mapping or drop rule, so the mapping had to be verified complete rather than assumed. The audit found 'Tproilf' -- a misspelling of 'Tprolif' in one dataset's source metainfo (330 cells). | The typo was given its OWN explicit drop rule rather than being normalised by fuzzy matching: a misspelling in upstream data is a fact about the data, and silently correcting it would hide that the vocabulary is unclean. 14 working classes (10 immune + 4 context), 57,926 cells (6.8%) dropped with a stated reason each. |
| 17 | 2026-07-31T21:53Z | 2 harmonise | ILC (innate lymphoid cells) dropped rather than merged into NK. | ILC are genuinely immune but appear in a single dataset at 202 cells corpus-wide. They cannot support a class in a cross-dataset design, and folding them into NK would assert a lineage equivalence the data does not establish. | Dropped and counted in T4 rather than merged. The count is reported so the choice is visible. |
| 18 | 2026-07-31T21:58Z | 2 harmonise | Accounting tables (qc_summary, T4) rebuilt from per-dataset sidecars instead of the rows produced by the current invocation. | REAL BUG found by testing resumability: on a resumed run, skipped datasets contribute no rows, and the whole-file overwrite reduced a 13-row qc_summary to a 1-row file while the stage reported success. Resumability was silently destroying the accounting. | Sidecars are written per dataset at the moment it is processed, and the stage now REFUSES to write a partial accounting table. Verified by running --force then a resumed run: 13 QC rows and 126 T4 rows survive both. |
| 19 | 2026-07-31T21:56Z | 2 harmonise | Cached-artefact resume check strengthened: a processed h5ad is trusted only if it opens AND carries the required obs columns. | The crashed first run left a BRCA_GSE161529.h5ad that existed at a plausible size but could not be opened by h5py. The original existence check trusted it, which silently dropped that dataset from all accounting. | Unreadable or incomplete caches are now deleted and re-derived. This is the second time in this project that an existence check accepted a truncated file; integrity is verified by opening, never by presence or size. |
| 20 | 2026-07-31T21:59Z | 2 harmonise | QC thresholds removed ZERO cells; documented as an inert guard with measured evidence rather than presented as a filtering step. | Measured on the raw matrix: minimum genes/cell is exactly 500 (a hard upstream floor), 1st percentile 520, and pct_mito tops out at 10.8% against a 20% threshold. TISCH2 distributes already-QC'd matrices. Three datasets (CRC_GSE108989, HNSC_GSE103322, LIHC_GSE98638) contain no MT- genes at all, so the mito filter is structurally inapplicable there. | Thresholds retained as an assertion that inputs are pre-filtered, not as a claim that filtering occurred. Recorded per dataset as mito_filter_applied and written up in results/qc_notes.md so the report cannot imply uniform QC. |
| 21 | 2026-07-31T22:05Z | 2 harmonise | PRIMARY macro-F1 is computed over classes present in BOTH the train and test partition of a given contrast; macro-F1 over ALL test-partition classes is computed and reported alongside it as a secondary metric in T2 and T3. | Class coverage is uneven across the corpus (CD8_T in 13/13 datasets, Neutrophil 2, Treg 3, Epithelial 3, NK 4). Under S3 and often S2 a test partition contains classes absent from training, which no classifier can predict and which score F1=0. That penalty is identical for every representation, so it adds equal noise to every arm and dilutes the representation contrast the study exists to measure -- it reflects corpus composition, not representation quality. | The evaluation class set varies per contrast, so absolute macro-F1 is not directly comparable ACROSS split schemes; the class set and its size are recorded per row in T2 so every number is interpretable. Both metrics are reported, so a reader can see whether the choice changed any conclusion. Decision taken before any result exists. |
| 22 | 2026-08-01T02:37Z | 3 embed | Geneformer obtained via the HuggingFace hub API rather than `pip install git+https://huggingface.co/...`. | git-lfs is not installed on this host, so the pip-from-git path fails when cloning an LFS-backed repo. The hub API fetches LFS objects over HTTP without git. | Checkpoint and dictionaries downloaded successfully. Snapshot commit 04c2b2e84da7c0f385c3f9ad8f3ec24bab6650e5 is pinned and all five files are SHA256-hashed. |
| 23 | 2026-08-01T02:40Z | 3 embed | FALLBACK TAKEN: the Geneformer checkpoint is loaded directly through transformers, and tokenisation is implemented from the published token/median dictionaries, instead of importing the `geneformer` package. | The package's __init__ imports its fine-tuning stack unconditionally (peft, ray, tensorboard, datasets). Those are fine-tuning dependencies and this study is strictly zero-shot, so they add no capability. Installing `datasets` already downgraded fsspec and put s3fs into a resolver conflict; continuing down that chain risked the environment that every baseline and statistic depends on, for features that are never used. | Time-boxed per the engineering rules rather than fought. The tokenisation must now be implemented and VERIFIED against the published rank-value encoding rather than trusted, which is documented as an obligation in the embedding stage. Checkpoint vocab (20,275) matches the token dictionary exactly, which is the first of those checks. |
| 24 | 2026-08-01T02:40Z | 3 embed | Cell embeddings will be mean-pooled hidden states over non-padding tokens; the BERT pooler output is NOT used. | The checkpoint has no trained pooler: transformers reports bert.pooler.dense weights as newly initialised. Using the pooler output would embed cells through a randomly initialised layer and silently degrade every Geneformer result. | Mean pooling over the attention mask is the published Geneformer cell-embedding convention and avoids the untrained layer entirely. |
| 25 | 2026-08-01T02:36Z | 3 embed | Geneformer-V2-104M (general) selected; the Geneformer-V2-104M_CLcancer variant explicitly NOT used. | The repository also ships a cancer-tuned checkpoint. Using it would violate the zero-shot requirement and would confound the pretraining-overlap audit, since its additional training data is cancer single-cell data of exactly the kind this benchmark evaluates. | Reported as the checkpoint identity in the run manifest so a reader can see which of the four available checkpoints was used. |
| 26 | 2026-08-01T02:36Z | 3 splits | 75 leakage-controlled splits persisted (S1/S2 x 5 seeds, S3 x 5 seeds x 13 holdout groups), each validated by assert_no_leakage BEFORE being written to disk. | A split that violates its own scheme must not be able to reach disk, where a later stage could consume it without re-checking. | classes_test_only is 0 in every split, so the primary learnable-class metric loses nothing relative to the all-classes metric under S1 and S2; under S3 the learnable set narrows to 7.5 classes on average, which is exactly the effect the metric decision was made to handle. |
| 27 | 2026-08-01T02:43Z | 3 embed | The reimplemented rank-value encoding is verified by five property checks that must pass before any embedding runs. | The encoding was reimplemented rather than taken from the reference package (see the fallback decision). A subtly wrong encoding does not raise an error: it produces embeddings that look reasonable, cluster plausibly, and silently misrepresent the model being benchmarked. Checks: rank order follows median-normalised value; the encoding is scale-invariant under CP10k; median scaling genuinely changes the ordering relative to raw counts (otherwise it would be plain count ranking); zero-count genes are excluded; sequences truncate to the model input size. | All five pass. The embedding stage refuses to run if any fails, so this cannot silently regress. |
| 28 | 2026-08-01T02:43Z | 3 embed | Geneformer vocabulary coverage recorded per dataset in T1 (mean 0.781, range 0.560-0.969). | Genes absent from the model's token dictionary are dropped during encoding. A dataset whose genes are poorly represented in the vocabulary is being embedded from a partial view of its own transcriptome, which is a caveat on that dataset's result rather than a property of the method. | NSCLC_GSE127465 (0.560) and LIHC_GSE140228_10X (0.637) are the weakest. Coverage is available as a covariate when interpreting any per-dataset Geneformer result, and is reported alongside the H4 gene-overlap analysis. |
| 29 | 2026-08-01T02:45Z | 3 embed | .gitignore rule `models/` replaced with the root-anchored `/models/`. | The unanchored rule was written to keep model WEIGHTS out of git, but it matched any directory named `models` at any depth -- including `src/scfmbench/models/`, the package holding the Geneformer encoder. The module was silently untracked while `git push` reported success and the remote verification compared matching commit hashes. | Caught by explicitly listing the expected files on the remote after the push rather than trusting the hash comparison alone. The repository would otherwise have been non-reproducible: the fresh-clone check in Phase H would have failed to import the embedding stage. Verifying that a push landed now means verifying the intended FILES are tracked, not only that the commits match. |
| 30 | 2026-08-01T13:05Z | 3 embed | anndata's backed sparse slicing replaced with a direct h5py CSR row-block read. | anndata 0.10.8 raises AttributeError: 'backed_csr_matrix' object has no attribute '_validate_indices' against the scipy in this environment -- the library's own backed-slicing path is broken. Pinning scipy down to satisfy it would disturb an environment that every baseline and statistic depends on. | Reading the indptr window and the exact data slice with h5py is also strictly leaner than instantiating a backed matrix per shard. Measured peak host RSS for the whole embedding pass: 19 MiB. |
| 31 | 2026-08-01T13:12Z | 3 embed | Geneformer embeddings are strongly ANISOTROPIC (mean pairwise cosine 0.954 on a 500-cell sample, p99 0.979). All representations will therefore be standardised with a scaler FIT ON THE TRAINING PARTITION ONLY before the classifier head. | Mean-pooled transformer hidden states occupy a narrow cone; raw cosine or Euclidean geometry is compressed. HVG+PCA output is centred and scaled by construction, so leaving the scFM embeddings unstandardised would handicap the foundation models relative to the baseline -- straw-manning in the direction that flatters the expected negative result, which is just as much a bias as the reverse. Guardrail 4 requires both arms be treated with equal care. | Identical preprocessing for every representation: per-feature standardisation fit on train, applied unchanged to test. This is a leakage-relevant fit, so it obeys the same train-only rule as HVG selection, PCA, scVI and Harmony. Norms are not collapsed (row norm mean 16.43, sd 2.13, min 12.25) so the embeddings do carry variation despite the high cosine floor. |
| 32 | 2026-08-01T13:13Z | 3 embed | batch_tokens raised from 8192 to 24576 for the full pass after measuring the probe. | The probe peaked at 978 MB VRAM of 15360 available and 19 MiB host RSS, so the GPU was roughly 15x underused while the binding constraint (host RAM) was untouched. Throughput was consistent across two datasets (46.8k and 48.9k tokens/s), making the projection reliable. | Projected full-corpus cost 1.9 h at the probe setting; the larger batch should reduce that. Token lengths are heavily truncated at the 4096 context (median 2748, p90 4096), so cost is dominated by the context cap rather than by cell count. |
| 33 | 2026-08-01T13:20Z | 3 embed | batch_tokens REVERTED from 24576 to the measured-safe 8192 after a CUDA OOM. | My own error, and worth stating precisely: the probe peaked at 978 MB VRAM, so I raised the token budget 3x on the assumption that VRAM scales with token count. It does not -- attention memory grows with sequence length SQUARED, and the probe datasets were short-sequence (median 1229 and 3629 genes/cell) while the full corpus contains many cells truncated at the 4096 context. A token budget calibrated on short cells does not transfer to long ones. | Cost one failed job and no data: shard caching meant the 9 shards already written were reused on resume, and the integrity check would have rejected any partial shard. PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True added to reduce fragmentation. The lesson is that a memory probe must exercise the WORST case (longest sequences), not the cheapest dataset -- extrapolating a linear rule from a favourable probe is how the earlier host stall happened too. |
| 34 | 2026-08-01T13:25Z | 3 embed | `ulimit -v` abandoned as a memory guard for GPU jobs; replaced with RLIMIT_RSS. | Measured: CUDA initialisation alone reserves 8.1 GiB of VIRTUAL address space, against the 10.5 GiB cap I had set. That left ~2.4 GiB for the model, PyTorch and data, so loading a 417 MB checkpoint failed with `CUDA driver error: out of memory` WHILE nvidia-smi reported the GPU entirely free (0 MiB used) -- an error message that points at the GPU when the real constraint was a host address-space limit I imposed. | Virtual address space is left unbounded for CUDA processes and RESIDENT memory is bounded instead, which is what actually competes for the 15 GiB host and what earlyoom watches. The ulimit -v approach remains correct for the CPU-only stages (it caught the Census over-read), so it is retained there. Two failed jobs, no data lost. |
| 35 | 2026-08-01T15:11Z | 3 embed | scGPT runs successfully with use_fast_transformer=False; NO fallback was needed and the 90-minute time box was not exhausted (~3 minutes of setup). | The brief expected flash-attn on a Turing GPU to be this phase's casualty. The released code path exposes a flag that selects standard PyTorch attention instead, so the dependency fight never started. scgpt was installed with --no-deps because its own pins would have moved torch and transformers, both of which are load-bearing; verified before and after that torch stayed 2.3.1 and transformers 4.44.2. | The study keeps BOTH foundation models rather than one, so guardrail 10 (never generalise from a single model) can be satisfied by design rather than by caveat. Measured 64.2 cells/s, 811 MB VRAM, 97.8% vocabulary coverage on the probe dataset. |
| 36 | 2026-08-01T15:11Z | 3 embed | scGPT checkpoint identity established by SHA256 agreement across three independently-hosted copies, NOT by repository authorship. | CORRECTION to an earlier claim in this log: I initially described the source repository as the scGPT authors' own lab repo. That was an inference from the organisation name and was NOT verified -- the repo carries no README, no card metadata, and zero downloads, and the corresponding author's actual lab namespace returned 401. Organisational identity on a model hub is not something I can verify, so it must not be asserted. What IS verifiable is byte identity: the weights were compared against independently-hosted copies, one of which is a mirror named in the project's own source document. | best_model.pt (205.4 MB, sha256 6cb5d451...72b922) and vocab.json (60,697 genes, sha256 acca93d1...e8580f) are BYTE-IDENTICAL across wanglab/scGPT-human, perturblab/scgpt-human and VirtualCell2025/scGPT_human. args.json differs between perturblab and the other two (training-run metadata, not weights) and is recorded as such. Three independent hosts agreeing bit-for-bit on the weights is evidence the artefact is the released checkpoint; the hosting organisation's identity remains unverified and is not claimed. The staged args.json records data_source as a CELLxGENE-derived corpus, which independently corroborates the pretraining-overlap audit's premise that Census presence is a meaningful proxy for scGPT exposure. |
| 37 | 2026-08-01T15:11Z | 3 embed | The two foundation models return embeddings on different scales and both are passed through the SAME train-fit standardiser. | Measured: scGPT returns L2-normalised embeddings (row norm 1.00 +/- 0.00) while Geneformer's mean-pooled states are unnormalised (norm 16.43 +/- 2.13). Comparing them, or comparing either to centred PCA output, without a common scaling would let an output convention masquerade as a representation-quality difference. | Every representation receives identical treatment: per-feature standardisation fit on the training partition only. Recorded so a reader can see the comparison is not confounded by output convention. |
| 38 | 2026-08-01T15:30Z | 3 embed | Provenance claims about model-hub repositories are restricted to what is checkable: file hashes and file contents, never organisational identity. | An audit of my own work caught me asserting that a HuggingFace organisation was the scGPT authors' lab, on the basis of the name resembling the corresponding author's. I had no evidence: the repo has no README, no card metadata and no download history, and my attempt at the namespace actually matching the author's lab returned 401. The claim had already been written into this log and into durable project memory, where future sessions would have inherited it as fact. | Corrected here and in memory. The checkpoint is now justified by SHA256 agreement across three independent hosts including a mirror named in the project source document -- a stronger claim than the one I originally made, and one I can actually support. Reproducibility is unaffected: the hashes are recorded, so a third party can confirm they measured the same weights. |
| 39 | 2026-08-01T15:40Z | 3 embed | Classical representations are refit PER SPLIT on that split's training partition, not cached once for the corpus. | A zero-shot foundation model embeds each cell independently of any partition, so its embedding is cacheable globally. HVG selection and PCA are FIT, so a single global fit would carry information from cells that later appear in test -- the exact leak guardrail 1 forbids. This asymmetry means the classical arm costs MORE to evaluate than the scFM arm, which is the opposite of the usual framing and is worth stating in the report. | 75 representation fits (5 S1 + 5 S2 + 65 S3), each written with the full cell index so the sweep can consume them like any other embedding. |
| 40 | 2026-08-01T15:42Z | 3 embed | PCA switched from IncrementalPCA over every training cell to randomized SVD on a 50,000-cell subsample of the TRAINING partition. | The exhaustive fit measured 978 s per split, which is 20 h for one baseline arm across 75 splits -- a wall-clock problem, not a scientific one. The subsample is drawn from train only, so the leakage contract is unchanged, and only the FIT is subsampled while the TRANSFORM is applied to every cell. Verified rather than assumed: against the exhaustive fit on synthetic data with latent structure, the two 20-dimensional principal subspaces agree to a mean cosine of 0.9961 (largest principal angle 6.3 degrees) at a 47x speedup. | The baseline is not weakened to save time (guardrail 4) -- it is computed with better linear algebra. Equivalence is checked by subspace alignment, not merely by explained variance, because two different subspaces can carry the same total variance. |
| 41 | 2026-08-01T15:43Z | 3 embed | The classical corpus is built on the INTERSECTION of gene symbols across all 13 datasets: 11,054 genes. | A union would pad each dataset with zero columns for genes it never measured, and a zero in an unmeasured gene is not an observation of absence. PCA fit on union-padded data would model each assay's gene panel rather than biology, and the resulting batch structure would flatter integration methods for the wrong reason. | The classical arm sees 11,054 genes where the scFM arm sees each dataset's own genes mapped into a 20,275-token (Geneformer) or 60,697-token (scGPT) vocabulary. This is a real asymmetry in the comparison and is recorded as a limitation rather than hidden: it slightly disadvantages the baseline, and the direction matters because the study's expected result is that the baseline wins. |
| 42 | 2026-08-01T16:22Z | 4 sweep | scikit-learn's `multi_class` keyword removed; multinomial softmax now ASSERTED after fitting instead of requested. | scikit-learn 1.9 removed the `multi_class` argument, so the pilot failed with a TypeError. With lbfgs, multi-class problems are now fit as multinomial softmax by default -- verified empirically (one coefficient row per class, probabilities summing to 1) rather than taken from a changelog. Relying on a default that could change again would silently switch the study to one-vs-rest, a different classifier, breaking guardrail 3. | The sweep now raises if the fitted model is not multinomial. The config still records the intent, annotated to say it is enforced by assertion rather than by keyword. |
| 43 | 2026-08-01T16:55Z | 4 sweep | Inner-CV hyperparameter selection subsampled to at most 20,000 training cells (stratified); the final model is still refit on the ENTIRE training partition with the selected C. | The mandatory pilot caught a cost problem that would have made the full run impossible: at the unrestricted label budget the training partition is ~138,000 cells, and 5-fold CV over a 6-point C grid is 30 logistic-regression fits on ~110,000 x 768 -- over 20 minutes per run, roughly 375 hours across the 1,125 unrestricted-budget cells of the grid. This is exactly what the pilot gate exists to find, and it found it before the full run rather than days into one. | Verified rather than assumed: on 60,000 synthetic cells the subsampled selection chose C=0.01 where exhaustive CV chose C=0.001 -- a DIFFERENT value -- but the two differ in held-out macro-F1 by 0.00005, which is 400x below the study's pre-specified negligible threshold of 0.02, because the CV score curve is flat near the optimum (correlation 0.95-0.99 with the exhaustive curve). What changes is the cost of selecting C. What does not change: the classifier, the unreduced C grid, the data the final model is fit on, or the leakage contract (the subsample is drawn from TRAIN only). Cells used for selection are recorded per run in T2 as cv_selection_cells. |
| 44 | 2026-08-01T19:05Z | 4 sweep | Sweep now honours `splits.scheme_ids` from the config instead of globbing every split file. | The pilot config declared a single split scheme, but the sweep globbed the split directory and ran all five seeds of it -- so the pilot was silently running five times its specified scope. A gate that does not run the scope it declares is not a gate. | The stage raises if no split matches the configured schemes, and `run.max_splits_per_scheme` bounds the pilot to the brief's 1 split / 2 budgets / 2 seeds. |
| 45 | 2026-08-01T19:06Z | 4 sweep | Per-split classical representations are loaded by split, never concatenated. | The embedding loader was written for the scFM arms, which cache one embedding per corpus sharded by dataset. Classical representations are FIT per split, so there are 75 files each containing all 229,801 cells; concatenating them stacked every cell 75 times and raised a duplicate-index error. Had the index happened to be unique it would have silently evaluated on 17 million rows. | The loader takes the split identity, and REFUSES to concatenate a directory whose files are named after splits rather than returning a duplicated index. |
| 46 | 2026-08-01T19:40Z | 4 sweep | HVG grid extended from [1000,2000,3000] to [2000,3000,5000,8000,0] where 0 means no HVG selection at all; PC grid confirmed already saturated at 100. | The pilot produced a result CONTRARY to the study's expectation -- both foundation models beat HVG+PCA (0.906/0.907 vs 0.839) -- so before accepting it I checked whether the losing arm was fairly configured, as guardrail 4 requires. It was not. Measured on the pilot split: macro-F1 rises monotonically with HVG count, 2000: 0.8225, 3000: 0.8353, 5000: 0.8406, 8000: 0.8503, all 11,054 genes: 0.8555. The grid's old ceiling of 3000 was costing the baseline 0.020 macro-F1, which is exactly the effect size this study pre-specified as meaningful. Two other suspicions were checked and CLEARED: PC count genuinely saturates (100: 0.8353 beats 200: 0.8278 and 300: 0.8265) and the C grid edge is not binding (flat past C=1.0). | The baseline is refit at the grid optimum before any full run. Had I scaled the 147-hour grid on the original configuration, the headline comparison would have rested on a baseline I had already under-tuned by a meaningful margin -- and the direction of that error flatters the foundation models, so it would not have been caught by the study's own expectation. |
| 47 | 2026-08-01T19:41Z | 4 sweep | Representation dimensionality is now SELECTED by inner CV on training data, not fixed at the grid maximum. | The config declared `n_pcs_grid` as a tunable grid, but the stage materialised only max(grid) and the sweep never chose among the points -- so the baseline's declared tuning never happened. Principal components are ordered, so the first k columns of a 100-component fit are exactly the k-component representation; selection needs no refit. Foundation-model dimensions are not variance-ordered, so those arms keep their native width and truncating them would be arbitrary. | The selected dimensionality is recorded per run in T2 as n_dims_selected, so a reader can see what each arm actually used rather than trusting that a declared grid was honoured. |
| 48 | 2026-08-02T09:30Z | 4 sweep | Every run is now scored PER DATASET, and the statistics stage reads an explicit `dataset` column with NO fallback. The full grid was restarted, discarding ~17.5 h of completed compute. | Guardrail 5 makes the dataset the unit of replication. Under S1 and S2 a single split spans all 13 datasets, so the sweep produced one pooled macro-F1 per run and no per-dataset breakdown existed anywhere in T2. The seed-aggregation step then fell back to the SPLIT FILE as the replication unit -- and the five S1 split files are five random resamplings of the same corpus. The paired test for H1 and H2 would therefore have run across 5 pseudo-replicates presented as independent units: the same disease the guardrail names, in a subtler form than counting 5 seeds x 13 datasets as n=65. Demonstrated on synthetic data with a true effect: the split-seed path reports n=5, CI [+0.0058,+0.0122], p=0.0625, while per-dataset scoring reports n=13, CI [+0.0026,+0.0154], p=0.0171 -- the defective path gives a NARROWER interval from FEWER real units, so it understates uncertainty on the headline claim. S3 was unaffected because its holdout group is a genuinely held-out study. | Learnable classes are now resolved per dataset, which is also more correct: a class absent from one dataset's test cells should not penalise that dataset. The classifier fit is untouched -- only the scoring of existing predictions is partitioned -- so the change costs essentially nothing per run. The fallback was deleted rather than fixed: a fallback that yields a plausible number is worse than a hard failure, because nothing downstream can distinguish them. Why it was missed: the statistics stage was unit-tested against synthetic frames that already carried a dataset column, so the fallback path was never exercised against the real T2 schema. |
| 49 | 2026-08-02T14:35Z | 4 sweep | A run is treated as complete on resume ONLY if all of its per-dataset rows are present; each row records `n_datasets_in_run` so completeness is checkable. | Per-dataset scoring made every run write 13 rows in a single append. Resume keyed on (representation, split, budget, seed), so a process killed mid-append would leave the run key present with a partial set of rows -- and resume would SKIP it. That run would permanently contribute a partial dataset panel, silently, and the statistics would test an unbalanced panel with no error anywhere. Verified by simulating a truncated write: the key was present in the resume set and the run would have been skipped at 7 of 13 datasets. | Rows written before this column existed are treated as unverifiable and recomputed rather than assumed complete. An incomplete run is now recomputed and its stale rows dropped when shards are merged. |
| 50 | 2026-08-02T14:40Z | 4 sweep | Sweep parallelised by SPLIT across N workers, each writing its own shard file; resume reads every shard. | The grid is ~90 h serial and dominated by the unrestricted-budget cells. Sharding by split rather than by representation keeps each worker holding one representation at a time, preserving the single-worker memory profile (~0.8-2.7 GiB) on a 15 GiB host that also runs an OOM killer. Separate shard files avoid concurrent appends to one CSV, which would interleave rows with no locking and no error. | Partition verified exact: with 75 splits and 3 workers every split is assigned once with no overlap. Resume globs all shards before the shard rename, so each worker sees the others' completed runs and re-running with different sharding never repeats work. |
| 51 | 2026-08-02T14:45Z | 4 sweep | Partial sweep results are committed and pushed to the remote on a fixed interval by scripts/checkpoint_results.sh. | Resumability protects against a crash; it does NOT protect against losing the instance, and the brief states access may end without notice. A 30-hour run with nothing pushed for 30 hours is a single point of failure that resumability does not address -- the operator raised exactly this. The results table is the deliverable, is a few MB at full size, and is not a regenerable intermediate in the sense the no-large-binaries rule targets. | Worst case becomes the loss of one checkpoint interval rather than the whole run. The script merges shards and admits only runs whose dataset rows are all present, so the committed table is always the same artefact the statistics stage consumes, and it verifies each push landed by comparing local HEAD against the remote ref rather than trusting a successful-looking push. |
| 52 | 2026-08-02T17:20Z | 4 sweep | Sharding across 3 workers measured, and it bought NOTHING: 3 single-threaded workers deliver 1.05x the throughput of one 4-threaded worker. | Per-run cost at the unrestricted budget rose from 309 s (4 BLAS threads, serial) to 884 s (1 BLAS thread, 3 workers) -- a 2.9x per-run slowdown against 3x concurrency. The work is dense CPU matrix algebra and the box has 4 cores; partitioning them does not create capacity. Load average sat at 3.06 on 4 cores, so the machine was saturated either way. My earlier 30 h projection was wrong; the measured figure is 93 h, of which 270 of 279 remaining serial hours are the unrestricted-budget cells. | Recorded rather than quietly dropped: the sharding infrastructure is still worth keeping because it makes the run restartable in independent pieces and the shard files avoid concurrent appends to one CSV, but the speed claim was not real and the report must not repeat it. |
| 53 | 2026-08-02T17:25Z | 4 sweep | The expensive fits were diagnosed before being optimised: they CONVERGE (630 of 2000 iterations), and the GPU was measured at 0 percent utilisation and 0 MiB throughout. | An iteration cap would have been the cheap explanation and would have been wrong. The cost is genuine dense linear algebra on 137,881 x 768 with 14 classes, which is exactly what the idle T4 is for. Confirming convergence first means the GPU port is a hardware change rather than a silent relaxation of the fit. | Establishes that moving the solver to GPU cannot change the answer by changing how hard the problem is solved -- only where it is solved. |
| 54 | 2026-08-02T18:40Z | 4 sweep | GPU multinomial logistic regression implemented and verified EXACTLY equivalent to scikit-learn; the equivalence gate's coefficient-correlation criterion was then removed because it rejected a correct implementation. | The objective is strictly convex, so it has one minimum. In fp64 both solvers reach it exactly: relative objective gap 3e-11 to 4e-09 and coefficient correlation 1.000000 at C = 0.01, 1.0 and 100.0. My initial gate demanded coefficient correlation >= 0.99 and FAILED the implementation at 0.9883 -- but the cause was scikit-learn stopping early at its default tolerance (objective 1873.79 versus the GPU's 1852.42 on identical data), so the GPU had found the BETTER optimum and the gate was rejecting it for out-converging the reference. Labels agreed on 99.7 percent of cells and macro-F1 differed by 0.0004. | The gate now tests what the study reports -- predicted labels (>=99 percent agreement) and macro-F1 (within 0.005) -- across a range of C, and reports coefficient correlation for diagnosis without failing on it. A wrong objective cannot satisfy both criteria across regularisation strengths. The gate runs on real data before any GPU fit is trusted, so guardrail 3 is checked rather than asserted. fp32 is used in production; fp64 was used only for the equivalence proof because a T4 runs fp64 at roughly 1/32 the fp32 rate. |
| 55 | 2026-08-02T18:30Z | 4 sweep | Operational error: a pkill -f with a broad pattern intended for one diagnostic also killed all three sweep workers. | The pattern matched the workers' command line as well as the diagnostic's. Recorded because this is the kind of mistake that looks like an infrastructure failure when read back later. | No data lost, which is precisely what the verified-resume work was for: on restart every recorded run was found complete, zero partial runs, and the sweep continued from 347 runs. Process kills must target a specific script name. |
| 56 | 2026-08-02T19:50Z | 4 sweep | Resume keys are built by one canonical function (_runkey) on BOTH sides of the comparison, normalising floats to integers and null budgets to 'all'. | Seeds were written as integers but pandas inferred a float column on read (one row with a missing seed is enough), so a stored key of ('geneformer', 'S1...parquet', '10', '0.0') never matched the loop's ('geneformer', 'S1...parquet', '10', '0'). ZERO of 350 completed runs matched their own resume keys: the sweep reported 'resuming: 350 complete runs' and then recomputed all of them, which is why no new runs appeared for 45 minutes. Verified by round-tripping 30 keys through CSV: 30/30 survive with normalisation, 0/30 with the previous ad-hoc str() casts on each side. | Wasted compute only -- no incorrect results, because a recomputed run produces the same answer and the append is keyed identically. But it silently negated resumability, which is the property the whole durability design rests on, and it would have made the run appear to hang. |
| 57 | 2026-08-02T19:45Z | 4 sweep | The sweep now checks whether any run for a (representation, split) is outstanding BEFORE loading that split's embedding matrix. | Loading a 229,801-row embedding takes 30-60 s. On a resumed run the loop paid that for every split whose runs were already complete, spending long stretches on pure I/O with nothing recorded -- indistinguishable from a stall when read from the log. | Resumed runs now skip completed splits immediately and print a [skip] line, so progress is visible and resume is cheap. |
| 58 | 2026-08-02T23:05Z | 4 sweep | Component profiling relocated the sweep's cost and corrected an earlier conclusion of mine: inner CV is 99.3% of an unrestricted-budget run (3096 s) and the final refit is 0.4% (13 s). | I had assumed the cost was the single fit on the full 137,881-cell training partition and optimised that first, which is why the GPU work produced no net speedup -- it was aimed at 0.4% of the run. The real cost is selecting C: 6 values x 5 folds is 30 fits, and 30 fits on 20,000 cells dominates one fit on 138,000. Measuring components before optimising would have found this immediately; I optimised on an assumption instead. | The inner CV now runs on the GPU solver as well. Measured in isolation on real embeddings (20,000 x 768, 14 classes, 6 C x 5 folds): 98.9 s against 356.3 s for scikit-learn, the SAME C selected, score-curve correlation 0.9924, maximum score difference 0.00305 across the grid -- far below the 0.02 negligibility threshold. Grid and fold structure unchanged. |
| 59 | 2026-08-02T23:10Z | 4 sweep | The design-reduction recommendation was changed from 'cut seeds at budget=all' to 'try the unreduced design first' in light of the profile. | The reduction was proposed when the projected cost was 120 h. With the inner CV on GPU the projection falls toward 35-45 h with the design fully intact. A post-hoc change to a pre-specified design is a real cost -- it belongs in the limitations section -- and taking it when it is not needed buys nothing. | The briefing keeps the reduction options, the power analysis and the measured variance structure so the decision can be made if the projection does not hold. It states explicitly that section 5's figures are computed against the 119.9 h baseline and must be re-derived once ~50 clean runs confirm the new per-run cost. |
| 60 | 2026-08-02T23:12Z | 4 sweep | Two power claims in the briefing were corrected: they contradicted the briefing's own tables. | An external review found that '0.01 of power at every effect size' and '0.26 at delta=0.05' did not match the printed power tables, which show 0.02/0.01/0.00 at deltas 0.02/0.03/0.05 for the cut at budget='all', and 0.31 rather than 0.26 at budget=5. The commit message of the same push carried the correct per-delta values, so this was a miscalculated summary rather than a disagreement about the numbers. | Both sentences now state per-delta values read directly off the tables, and the recommendation quotes 'at most 0.02'. The direction of the analysis is unchanged. Recorded because a summary sentence that overstates a table is exactly the kind of error a reader cannot catch without recomputing. |
| 61 | 2026-08-02T23:40Z | 4 sweep | The GPU inner CV is confirmed in the running sweep at 186 s per unrestricted-budget run, so NO design reduction is being taken. | Measured over 30 completed runs against 357 s with the GPU on the final refit alone and 309 s in the original all-CPU configuration; GPU utilisation is 40 percent rather than 0. Full-grid projection falls from ~120 h to ~71 h with every axis intact -- 3 representations x 75 splits x 6 budgets x 5 seeds, the unreduced C grid, 5 inner folds, and every leakage control. | The reduction options and their power analysis are retained in results/cost_and_design_options.md in case the cost regresses on the S3 splits, which have larger training partitions (158,306 versus 137,881 cells) and are 65 of the 75 splits -- the one place the projection could still move materially. Against the 71 h baseline Option A would now save ~26 h rather than ~67 h, for the same power cost. |
| 62 | 2026-08-03T19:20Z | 4 sweep | EXTERNAL REVIEW UPHELD: scVI and Harmony are mandatory baselines in the brief and are NOT running. The config declares enabled: true for both but no implementation and no embeddings exist. | Verified: configs/default.yaml lists scvi, harmony and celltypist as enabled, classical.py names them in its docstring, but the embeddings directory contains only geneformer, scgpt and hvg_pca. The brief puts scVI and Harmony in a <baselines MANDATORY="true"> block, and H2 is stated as scFMs matching or exceeding scVI and Harmony -- so H2 as written CANNOT be tested in the current configuration. The brief also cites Wu et al. on scVI's limited cross-dataset transferability as a substantive comparison for H2, which is precisely the S3 regime. Harmony is the only baseline designed for the distribution shift being tested. A config flag reading enabled: true while nothing implements it is worse than an honest omission, because it makes the gap invisible. | The config flags are the defect: they asserted capability that does not exist. Either the two arms are implemented and the sweep extended, or the flags are set false and the omission is declared prominently as a limitation with H2 restated as testable only against HVG+PCA. This is an operator decision because it is a scope-versus-completeness trade, not a correctness question. |
| 63 | 2026-08-03T19:25Z | 4 sweep | EXTERNAL REVIEW UPHELD and STRENGTHENED: the H2 cell (low label budget x leave-dataset-out) is underpowered, and measurement on real S3 runs shows it is worse than the reviewer inferred. | The reviewer used the S1 budget=5 seed SD (0.0780) as a proxy. Measured directly on 1,935 clean S3 rows, seed SD in the H2 cell is HIGHER: 0.0898 at budget=5, 0.0778 at 10, 0.0622 at 25, 0.0514 at 50. Simulated power at n=13 datasets with eff_sd=0.03: at budget=5, delta=0.03 gives 0.32 and delta=0.05 gives 0.70. So the study's most novel claim is tested at roughly one-third power for an effect it calls meaningful. The brief's reporting discipline requires distinguishing 'no evidence of difference' from 'evidence of no difference', and at this power that distinction cannot be made -- a null H2 would be uninterpretable rather than informative. | The reviewer's proposed fix follows from the project's own measured logic and is the best marginal use of compute available: seeds are load-bearing exactly where compute is cheap. Adding seeds in the H2 cell lifts power at delta=0.03 from 0.32 to 0.48 (10 seeds), 0.64 (20) and 0.76 (40); at delta=0.05 from 0.70 to 0.89/0.97/1.00. Measured cost of quadrupling seeds (5 -> 20) in the H2 cell is +20.2 h. Unlike a reduction this INCREASES replication, so it does not weaken any pre-specified analysis -- but it is still a post-hoc design change and must be declared as one. |
| 64 | 2026-08-03T19:28Z | 4 sweep | EXTERNAL REVIEW REBUTTED on the calibration partition: it is intact and untouched. | Verified directly: S1 seed0 is train 137,881 (60.0%), test 45,963 (20.0%), calibration 45,957 (20.0%) -- exactly the mandated 60/20/20, so the 137,881 figure the reviewer flagged as 'high for a 60% partition' is 60.0% of 229,801. The sweep calls assert_calibration_untouched({'train','test'}) before every split is used and never reads the calibration partition. The concern was well-aimed (silent absorption would fail quietly and would not affect H1/H2) but the guard exists and the arithmetic checks out. | Recorded because the reviewer was right that this is a high-consequence, quiet failure mode; the check is cheap and now documented rather than assumed. |
| 65 | 2026-08-03T19:30Z | 4 sweep | EXTERNAL REVIEW REBUTTED on the duplicate-accession resolution: it was fully resolved, and the 13 vs 12 arithmetic has a different explanation than the reviewer supposed. | Verified from T1: 13 datasets with 13 DISTINCT ldo_group values and zero datasets sharing a group. Neither GSE143423 nor GSE150660 -- the two accessions the brief flagged -- is in the final corpus at all: both were excluded by the pre-specified selection criteria (>=3 donors, >=3 mappable immune classes, >=500 immune cells) applied blind to the hazard. The corpus is 13 datasets because it was EXPANDED beyond the original 14 local entries by fetching from the archive to give LIHC adequate representation, not because two duplicates resolved to 12. Screening the expanded pool also caught two further hazards the brief did not know about (GSE125449 across two cancer types, GSE140228 across two assays), both handled by making the accession the S3 holdout unit. The 75 split files confirm 13 distinct holdout groups named by accession. | S3 does not have same-study cells on both sides of any split. The reviewer's inference from 14 -> 12 was reasonable from the brief alone but the corpus composition changed upstream. |
| 66 | 2026-08-03T19:32Z | 4 sweep | EXTERNAL REVIEW RESOLVED FAVOURABLY on the S3 cost regression: S3 runs are FASTER than S1, not slower. | The reviewer flagged S3's larger training partitions (158,306 vs 137,881 cells) as the place the cost projection could regress, and I had recorded the same risk. Measured on 1,935 completed S3 rows: budget='all' averages 101 s on S3 against 112 s recent overall, because the S3 test partition is much smaller (17,677 vs 45,963 cells) and the inner-CV subsample is capped at 20,000 either way. | The 71 h projection holds and the Option A fallback is not triggered. The trigger condition is now moot for cost reasons, though the reviewer's separate point about pre-committing contingency triggers stands on its own merits. |
| 67 | 2026-08-03T19:55Z | 4 sweep | EXTERNAL REVIEW TESTED AND NOT SUPPORTED: subsampled C selection does not bias the comparison, and the one shift observed runs opposite to the direction predicted. | The reviewer's mechanism is real -- sklearn's objective is C*sum(loss) + 0.5*||w||^2, so the optimal C does shift downward as n grows, and they reasoned a C chosen at 20,000 cells would under-regularise at 137,881 and hurt the 768-dimensional arm most, i.e. in the direction that flatters the study's expected negative result. Measured on S1 seed0 by running the same 5-fold selection on 20,000 cells versus the full 137,881-cell training partition: geneformer (768-d) selects C=0.1 BOTH times, scgpt (512-d) selects C=1 both times, and hvg_pca (100-d) is the ONLY arm that shifts (0.1 -> 1). Test macro-F1 consequence: geneformer 0.0000, scgpt 0.0000, hvg_pca +0.0007 in favour of full-size selection. | Not supported on this data: the scFM arms are unaffected and the only arm that shifts is the baseline, which GAINS 0.0007 -- far below the 0.02 negligibility threshold and opposite to the bias feared. Subsampled selection stays. Worth stating in the report because the mechanism is sound and a reader may raise it; the answer is that it was tested rather than assumed. Caveat: one split, one scheme (S1 seed0). |
| 68 | 2026-08-03T19:57Z | 4 sweep | A non-unique-index error in my own ad-hoc diagnostic confirmed the pipeline loader guard is doing real work. | My first attempt at the C-selection test loaded hvg_pca by concatenating its per-split .npz files, which raised InvalidIndexError because each of the 75 files embeds the FULL 229,801-cell corpus, so concatenating all of them yields 75 x 229,801 = 17,235,075 rows for 229,801 unique cell_ids. (The 689,403 figure quoted in an earlier version of this entry was wrong: it came from a separate three-file probe, 3 x 229,801, and was conflated with the all-files load.) That is exactly the failure mode load_embedding() was hardened against after it corrupted an earlier sweep, and its refusal to concatenate a directory whose files are named after splits is why the pipeline cannot make this mistake. | The corpus index itself is clean: 229,801 rows, 229,801 unique cell_ids, zero duplicates. The test was re-run through the pipeline own loader. Recorded because a diagnostic written outside the stage modules can bypass a guard the pipeline relies on -- diagnostics should import the loader, not reimplement it. |
| 69 | 2026-08-03T23:00Z | 3 embed | MEASURED CONSTRAINT: TISCH2 distributes log1p(CP10K), not raw UMI counts, so scVI is fed depth-normalised pseudo-counts round(expm1(x)) applied UNIFORMLY to all 13 datasets. | scVI's negative-binomial likelihood requires integer counts and silently produces garbage on log-normalised input. Measured: per-cell sum(expm1(x)) is exactly 10,000 for every dataset. Testing invertibility gave a SPLIT answer -- for 9 of 13 datasets every value in a cell is an exact integer multiple of that cell's smallest nonzero value, so true UMI counts are exactly recoverable (implied library sizes 1.3k-4.1k); for the other 4 they are not (implied library sizes 337k, 297k, 49M -- plate-based TPM, where integer counts do not exist). Using true counts only where available would give scVI genuine counts on 9 datasets and degraded input on 4, putting a per-dataset artefact directly into a paired contrast whose unit of replication IS the dataset. | Uniform treatment keeps the confound out of the comparison at the cost of a declared approximation: every cell has library size ~10,000 by construction, so scVI's library-size latent carries no information. scVI's numbers are a LOWER BOUND on what it would achieve with true counts, and this is a limitation of the data source rather than of scVI. Must appear in Limitations. |
| 70 | 2026-08-03T23:05Z | 3 embed | DECLARED LEAKAGE DEVIATION: Harmony is fit TRANSDUCTIVELY over train+test PCA coordinates, because harmonypy 2.0 has no out-of-sample projection. | The brief requires Harmony be fit on train only then applied to test. Verified that harmonypy 2.0's Harmony class exposes K, R, Y, Z_corr, Z_cos, Z_orig and NO transform or project method. Three options were weighed: (1) fit on train and leave test uncorrected -- train and test would occupy different spaces, crippling a mandatory baseline for reasons unrelated to its merits, which is straw-manning; (2) implement a Symphony-style projection -- correct in principle but harmonypy does not expose the per-cluster correction terms, so it would be a bespoke reimplementation whose subtle errors would look like results; (3) run Harmony transductively on the batch key only, never labels. | Option 3 taken. TWO consequences must be stated in the report, not buried: it ADVANTAGES Harmony (it sees test-cell expression, though never labels) and Harmony is a BASELINE, so the bias points toward 'classical methods win' -- the study's own prior expectation, and therefore the most dangerous direction for an artefact. It must not be read as evidence for H1. Mitigating context: under S3 the held-out dataset is a batch never seen inductively, and transductive correction is the practical situation Harmony is actually used in. PCA remains train-only; only the correction step sees test coordinates. |
| 71 | 2026-08-03T23:15Z | 3 embed | A wrong-orientation bug in the Harmony wrapper was caught by the smoke test and turned into an assertion. | harmonypy 2.0 returns Z_corr as (n_components, n_cells) -- the transpose of its input. My first wrapper transposed unconditionally, which happened to be right, but the smoke test compared shapes and exposed that I had it backwards in the diff computation. Had the corpus been square, or had a future harmonypy version changed orientation, cells and components would have been silently swapped into a plausible-looking matrix of nonsense. | The wrapper now checks the returned shape against the input, transposes only when the shape is exactly the transpose, and RAISES rather than guessing on anything else. Verified: output (12000, 30) matches input, mean absolute change from the uncorrected PCA is 0.0958 so the correction is real, and the single-batch guard returns the input unchanged with skipped=True. |
| 72 | 2026-08-03T23:20Z | 4 sweep | EXTRA SEEDS ADDED IN THE H2 CELL ONLY: seeds 5-19 for leave-dataset-out at budgets 5/10/25/50, taking that cell from 5 to 20 seeds. | Measured on real S3 runs, seed SD is 0.0898 at budget=5 against 0.0137 at budget='all' -- because at a capped budget the seed decides WHICH labelled cells you get, which is the label-efficiency question the study exists to answer, while at 'all' every cell is used. That left H2, the study's most novel claim, at 0.32 power for a delta of 0.03: a null result there would be uninterpretable, which the brief's reporting discipline explicitly forbids passing off as evidence of no difference. Seeds are load-bearing exactly where compute is cheap, so this is the best marginal use of compute available. | Power at delta=0.03 rises from 0.32 to roughly 0.64 at 20 seeds (ceiling 0.90, set by n=13 datasets). The change is cell-scoped and ADDITIVE, so it weakens no pre-specified analysis: seeds are averaged to a dataset mean before any test, and a cell with more seeds simply has a less noisy mean. It is nonetheless a change made after seeing data and is reported as post-hoc in the Limitations section, not only in this log. |
| 73 | 2026-08-03T23:30Z | 3 embed | The representation-fit stage and the sweep must NOT run concurrently on this host; the sweep was stopped so the fit could proceed alone. | The representation fit materialises the full 229,801 x 11,054 corpus and was measured holding 12 GiB; with the sweep's 1.5 GiB alongside, free memory hit 0 of 15 GiB on a box that runs an OOM killer preferring python processes. That is precisely the failure mode the brief names as a design error rather than a tuning problem. The sweep is fully resumable and verified so, therefore stopping it costs at most the run in flight, whereas an OOM kill would take both processes. | Memory recovered to 4 GiB free with the fit alone at 9 GiB. The sweep resumes once both new representations are fitted. The checkpointer keeps pushing partial results throughout, so the pause does not weaken off-instance durability. |
| 74 | 2026-08-03T23:35Z | 4 sweep | Revised cost after adding two representations and the H2 seed boost: approximately 129 h remaining, against 71 h for the previous 3-representation 5-seed design. | Measured harmony 136 s per split (2.8 h for 75); scVI estimated ~600 s per split (12.5 h) and to be replaced with a measurement once fits land. Sweep grows from 3 to 5 representations, and the H2 cell from 5 to 20 seeds: S1+S2 all budgets 15.7 h, S3 at budgets 100 and all 90.8 h, S3 at budgets 5-50 with 20 seeds 45.0 h. | The increase is what the operator authorised and buys two things the previous design could not deliver: H2 testable as written against scVI and Harmony rather than HVG+PCA alone, and roughly four times the replication on the study's most novel claim. Recorded so the report states measured cost rather than the superseded 71 h figure. |
| 75 | 2026-08-04T00:55Z | 3 embed | scVI is restricted to 2,000 train-selected HVGs and a 60-epoch budget with early stopping, not the full 11,054-gene space and 200 epochs. | My first implementation passed every common gene to scVI. Measured consequence: one split ran past 55 minutes without finishing while spilling to swap on a 15 GiB host (1.7 GiB of swap in use at the time it was sampled; by the later OOM kill swap had reached 6.4 GiB of 8 GiB), and the GPU sat at ~59% -- host memory, the brief's named binding constraint, was the bottleneck. Restricting to HVGs is also what scVI is standardly trained on and matches what the brief already specifies for scGPT (top-3000 HVGs), so this is correct practice rather than only a cost fallback. Measured on 2,000 HVGs at the full 137,881 training cells: 9.7 s/epoch, so 200 epochs would be 32 min/split and ~40 h across 75 splits against ~12 h at 60 epochs. | HVGs come from the same train-only selector every other arm uses, passed in rather than recomputed so it cannot drift. Measured steady-state cost: scVI 607 s/split, Harmony 119 s/split, swap fully drained. The 60-epoch cap is a documented compute fallback and is recorded in the config comment as well as here; early stopping usually halts sooner. |
| 76 | 2026-08-04T01:00Z | 4 sweep | Revised cost, now fully measured rather than estimated: representation fits 15.1 h, sweep 151 h, remaining total roughly 133 h. | Earlier figures carried a ~600 s/split estimate for scVI; it is now measured at 607 s, so the estimate held. Sweep at 5 representations: S1+S2 all budgets 15.7 h, S3 at budgets 100 and all 90.8 h, S3 at budgets 5-50 with 20 seeds 45.0 h. | Roughly 5.5 days, against 71 h for the superseded 3-representation 5-seed design. That is the price of H2 being testable as written against scVI and Harmony rather than HVG+PCA alone, plus fourfold replication on the study's most novel claim. The report must quote these measured figures, not the 71 h. |
| 77 | 2026-08-04T13:20Z | 3 embed | The representation fit was OOM-killed at 7 of 75 splits and must be restructured to hold less memory, not merely restarted. | Confirmed from the OOM daemon's own log rather than inferred: at 02:04:53, with only 1,488 of 15,783 MiB memory available (9.43 percent) AND swap already 80 percent consumed (1,634 of 8,191 MiB free, 19.95 percent -- just past the 20-percent-free SIGTERM threshold), it sent SIGTERM to the fit process (VmRSS 12,921 MiB). Both limits were breached simultaneously, which is what the daemon is configured to catch: watching available memory alone would have missed how far the box had already spilled to swap. The rep-fit materialises the full 229,801 x 11,054 corpus plus a dense PCA/Harmony working set, and scVI's dataloader on top of that crosses the threshold. This is the failure the brief names as a design error rather than a tuning problem, and I had already stopped the sweep to make room -- the fit alone is still too large. | No data was lost, which is what the integrity design is for: all 7 harmony and 7 scvi files open, carry 229,801 cell ids and finite values, and each has a stats sidecar, so a resumed run skips exactly those. The fix is to reduce resident memory rather than retry: harmony and scvi are fitted in SEPARATE invocations (so only one working set is live), and scVI's HVG-restricted matrix is built and the full corpus released before training starts. |
| 78 | 2026-08-04T19:20Z | 4 sweep | SEEDS REDUCED FROM 5 TO 2 AT budget='all' ONLY (post-hoc, operator-approved, measured). All capped budgets keep 5 seeds; the H2 cell keeps 20. | At the unrestricted budget the seed does not change which cells are trained on -- verified on completed runs, n_train_used is identical across seeds in 112 of 112 (representation, split) cells. It only reshuffles inner-CV folds and the solver's random_state, so those replicates measure SOLVER AND FOLD noise rather than the label-selection noise the study exists to characterise. At every capped budget the seed does decide which labelled cells you get, which is why seeds are untouched there. That one cell was 58 h of a 136 h sweep. Measured impact on the dataset mean, which is the unit of replication: 2 seeds versus 5 shifts it by 0.00038 typically and 0.01515 at worst, below the pre-specified 0.02 negligibility floor; 1 seed reaches 0.02180, above it, so 2 is the floor of what is defensible rather than an arbitrary choice. | Saves roughly 35 h, taking the remaining timeline from 4.9 to 3.8 days without touching the H2 replication the operator authorised. Expressed as config (run.reduced_seeds) rather than a code literal, with two guards that raise: n_seeds below 2 is refused with the measured reason, and a budget appearing in both reduced_seeds and extra_seeds is refused rather than resolved silently. T3 now reports n_seeds per arm so a reader sees the replication behind every number without consulting the config. Declared in Limitations as a post-hoc change. |
| 79 | 2026-08-04T19:25Z | 5 stats | T3 reports the seed count behind each arm of every contrast (n_seeds_a, n_seeds_b), as a range when it varies. | Seed counts are deliberately non-uniform across the design after two post-hoc changes -- 20 in the H2 cell, 2 at the unrestricted budget, 5 elsewhere. A reader encountering a delta in T3 could not otherwise tell how much replication stood behind it without cross-referencing the config, and both changes are exactly the kind a reviewer should be able to audit from the results table alone. | Verified the addition does not alter any statistic (identical delta with and without the column) and that a T2 lacking n_seeds still produces a valid contrast rather than failing. Reported as a range like '2-20' when the two arms or datasets differ. |
| 80 | 2026-08-04T19:52Z | 3 embed | SECOND OOM kill on the scVI fit at 8 of 75 splits. Diagnosed as per-split accumulation, not a high baseline, and fixed structurally rather than by another in-loop cleanup. | Measured rather than guessed. The stage baseline is 8.11 GiB before any split work: the corpus CSR is 2.49 GiB at 334M nonzeros and the stage holds TWO full copies -- the raw log1p(CP10K) matrix that scVI's pseudo-counts derive from, and the renormalised matrix every other arm uses. The kill came at 12,849 MiB RSS, so per-split allocations add roughly 4.7 GiB against about 7 GiB of headroom, and the OOM daemon's own log shows available memory falling 40 percent to 23 percent to 8.5 percent across about two splits. That trajectory is accumulation per split, which is exactly what my previous fix (in-loop del plus gc.collect) was supposed to prevent and did not. | Rather than keep hunting an allocation inside lightning/scvi-tools that is not in my code, scVI now runs ONE SPLIT PER PROCESS via scripts/fit_scvi_isolated.sh. The OS reclaims everything at process exit, so peak RSS is bounded by a single split's requirement and the accumulation cannot occur by construction. Cost is re-reading the corpus per split, measured at 24 s against roughly 650 s of training -- under 4 percent overhead for a hard memory bound. Verified over 25 minutes: peak RSS 10.4-10.6 GiB per split (against 12.85 at the kill), zero OOM kills, zero failed splits, progress resumed from 8 to 10. The in-loop path is unchanged and still correct; it simply is not the entry point for scVI. |
| 81 | 2026-08-04T20:20Z | 3 embed | The per-split wrapper's resume gate validates each shard by OPENING it and checking the full cell index, not by testing for file existence. | A wrapper that skips work based on a filename would silently accept a shard truncated by the very OOM kill it exists to survive. This project has already been bitten twice by trusting existence and plausible size, on raw .h5 and on processed .h5ad. | The gate loads each candidate .npz and asserts 229,801 cell ids with all-finite embeddings; anything failing that is refit. Failed splits are counted and reported at the end rather than being swallowed, and exit code 137 or 143 is called out explicitly in the log as an OOM kill so a future reader does not mistake it for a code fault. |
| 82 | 2026-08-04T22:15Z | 3 embed | The per-split scVI fit runs under a supervisor with a bounded retry budget (3 consecutive failures) that EXITS with a written verdict rather than retrying indefinitely. | Both prior OOM kills cost roughly six hours of silent stall each. No work was lost -- every shard is integrity-checked and resumable -- but wall-clock was, because nothing noticed the process had died until a human asked for status. The supervisor polls shard count, restarts within one poll interval, and clears its failure count on real progress, since the failures that matter are consecutive rather than one transient across a multi-hour run. | The budget is deliberately small because the operator's decision rule is that repeated failure here opens the question of dropping scVI and declaring the omission, with H2 reported against HVG+PCA alone. A supervisor that retried forever would silently override that decision instead of surfacing it, so it writes scvi_verdict.txt naming the shard count, the restart count and the applicable fallback, then exits non-zero. Verified live: it detected the stopped fit at 10 of 75, restarted it, observed progress to 11, and cleared the count. |
| 83 | 2026-08-04T20:45Z | 0 setup | CORRECTION to an earlier recorded finding: the binding hardware constraint for the sweep is GPU availability and CPU MEMORY BANDWIDTH, not the number of CPU cores. The GPU must not be traded for more CPU or RAM. | Measured on real embeddings at real scale (137,881 x 768 training matrix, 14 classes), one classifier fit takes 14.8 s on the T4 against 286.4 s on 4 CPU threads -- 19.4x. CPU scaling saturates at TWO threads (285.2 s at 2 versus 286.4 s at 4), which identifies the CPU path as memory-bandwidth bound rather than core bound, so additional vCPU on the same memory subsystem would buy almost nothing. An earlier note recorded cores as the binding constraint; at the whole-pipeline level that was the wrong diagnosis and is corrected here. A first synthetic version of this measurement returned 0.9 s per fit and was discarded as invalid -- random labels converge immediately, so it was not measuring the real optimisation. | Remaining work is roughly 90 h with the GPU against roughly 1,571 h (65 days) CPU-only; scVI alone would go from 11 h to 37 h. Both foundation-model embedding passes are already complete and verified (229,801 of 229,801 cells each), so the GPU's remaining value is entirely in the classifier sweep. The scaling axis that would help is a SECOND GPU instance -- the sweep shards cleanly by split file and is already resumable with per-run integrity checks -- not a larger CPU-only box. |
| 84 | 2026-08-05T15:00Z | 3 embed | All five representations are complete and validated: 75 of 75 shards each for hvg_pca, harmony and scvi, all 225 opening cleanly with the full cell index and finite values. The sweep is resumed with five arms. | Validated rather than counted: every shard was opened and checked for 229,801 matching cell ids and all-finite embeddings, dimensions are uniform within each arm (hvg_pca and harmony 100, scvi 30), each has a stats sidecar, and no split file is missing from any arm. scVI finished with zero failed splits and zero OOM kills under the per-split isolation wrapper, peaking at 12.09 GiB against the 12.85 GiB level that killed the previous design. | The supervisor recorded exactly one restart -- its initial pickup of the fit I had paused for the CPU-versus-GPU benchmarks -- and its retry budget was never approached, so the operator's decision rule on dropping scVI was never triggered. |
| 85 | 2026-08-05T15:05Z | 3 embed | LIMITATION: every one of the 75 scVI fits ran the full 60 epochs; early stopping never triggered. scVI's numbers are therefore a lower bound for a SECOND, independent reason beyond the pseudo-count input. | early_stopping=True was configured, but epochs_run is exactly 60 in all 75 sidecars. That means the epoch budget rather than convergence terminated training, so this study has not established that scVI converged, and the arm cannot be read as scVI-at-its-best. The budget itself was a measured compute fallback (200 epochs would have been about 40 h against 12 h at 60). | Must appear in Limitations alongside the pseudo-count caveat, and the two must be stated together: if scVI loses to HVG+PCA, TWO data-source and compute artefacts point in that direction, so the honest reading is that this study does not establish scVI's ceiling. Stating only one of the two would understate the caveat. Also recorded: the batch covariate was used in 10 of 75 fits and dropped in 65, which is the expected pattern under leave-dataset-out, where the held-out dataset is a batch the training partition does not cover. |
| 86 | 2026-08-06T11:35Z | 5 stats | The pre-specified statistics stage was run end-to-end on the two COMPLETE schemes (S1, S2) while the grid continues, as a pipeline verification -- not as a preview to tune against. | S1 and S2 finished first, so waiting for the remaining 33 h of leave-dataset-out before ever exercising the stats stage on real data would risk discovering an integration defect at the end, when there is least time to fix it. To keep the check honest the stats module's hash and its commit were recorded BEFORE the run, so the record shows the analysis code was already frozen (committed at 02bc229) and not adjusted after results were visible. | The stage works end-to-end: aggregation returns 13 dataset units per cell, the paired contrast produces delta, bootstrap CI, Wilcoxon p, and Cohen's dz, and the replication unit is the dataset in every cell. One defect was found and fixed (below). No analysis choice was changed on the basis of the numbers produced. |
| 87 | 2026-08-06T11:40Z | 5 stats | REPORTING DEFECT FIXED: the aggregated count column was named n_seeds but counts RUNS (split-partitions x label-seeds). Renamed to n_runs, and the seed-SD column to macro_f1_resample_sd. | Under S1 with 5 split files and 5 label seeds the column reads 25, not 5. The arithmetic was always right -- both split partition and label seed are resampling noise, and both are averaged away before the dataset-level test -- but the name invited a reader of the primary results table to believe the design ran 25 seeds per cell. Found only by running the stage on real data and questioning a number that looked wrong. | A misleading column name in a published results table is a reporting defect even when the computation behind it is correct, and this one would have appeared in T3. The contrast output fields are renamed to n_runs_a and n_runs_b to match. The config knob run.reduced_seeds.n_seeds keeps its name because it genuinely sets a seed count. |
| 88 | 2026-08-08T02:00Z | 5 stats | Design grid COMPLETE (29,961 runs, zero incomplete cells, all 65 leave-dataset-out splits, 13 datasets) and the frozen statistics stage run over it: 108 contrasts, BH-FDR corrected. | Completeness was verified cell by cell against the seed policy rather than by a total count, and integrity re-checked: zero partial runs, zero null macro-F1, zero null dataset. The stats module's hash and commit (809a31b) were recorded before the run, so the record shows the analysis was frozen before any full-grid result was visible. | 49 of 108 contrasts significant at FDR 0.05, none of them flagged negligible. The pattern is scheme-dependent and is the study's central finding. |
| 89 | 2026-08-08T02:03Z | 5 stats | The pre-specified SECONDARY metric (macro-F1 over all test classes) was missing from T3, which reported only the primary. It has been computed through the identical pipeline and emitted as T3_secondary_all_test_classes.csv. | The pre-specification requires both metrics reported, and the omission mattered specifically because the two are identical under S1 and S2 (classes_test_only is 0 by construction) and differ ONLY under leave-dataset-out -- which is exactly where the headline result sits. Reporting one metric there without showing the other would leave the central claim resting on an unexamined analysis choice. | Result: 0 sign flips across 108 contrasts, 1 significance change at the FDR 0.05 boundary (geneformer - hvg_pca at budget 10 under S3, p 0.0693 primary versus 0.0564 secondary -- non-significant either way), and a maximum absolute difference in delta of 0.00102. The conclusions are robust to the metric choice, and both tables ship. |
| 90 | 2026-08-08T12:30Z | 5 stats | DESIGN OVERSIGHT FOUND: the sweep never persisted per-class F1, which H3 (cell-type rarity) requires. Recovered by a rescoring pass (s05b_perclass) that refits at the ALREADY-SELECTED C rather than re-running the 53 h grid. | metrics.evaluate computes per_class_f1, but the sweep wrote only the macro-averaged summary to T2, discarding the per-class detail 29,961 times. Re-running the grid would cost about 53 h and was unnecessary: T2 records C_selected for every run, so the model can be refit with NO inner cross-validation. That matters beyond cost -- because nothing is selected in the rescoring pass, it cannot introduce hyperparameter selection on test, which is the guardrail that would otherwise make a rescoring pass dangerous. | The refit reuses the identical split, budget subsample (same seed, same budget_indices), representation, train-only standardiser and C, so it must reproduce the macro-F1 already in T2. That equality is ASSERTED per run rather than assumed, and a mismatch marks the run failed instead of contributing a wrong per-class row. Smoke test on 6 runs: 588 class-rows, ZERO reconstruction failures, class prevalence spanning 0.0097 to 0.658. Scope is stated up front -- both scFMs plus hvg_pca at budgets 10 and 'all' across all three schemes -- rather than chosen after seeing which subset gives a cleaner answer. |
| 91 | 2026-08-08T12:33Z | 5 stats | Two errors in the first version of the rescoring stage were caught by running it: I invented a cfg["paths"]["data_root"] key that does not exist, and called fit_standardiser(E, tr_idx) when it takes the training matrix. | Both came from writing against a remembered interface instead of reading the one the sweep actually uses. The config error failed loudly; the standardiser error would have failed loudly too, but a subtler version of the same mistake -- fitting the standardiser on the wrong rows -- would have produced plausible numbers that quietly disagreed with T2. | Fixed by reading the sweep's own key names (cfg["data"][...]) so the rescoring pass cannot point at a different directory tree than the run it is reconstructing. This is also why the reconstruction assertion exists: it is the check that catches the class of error that does not throw. |
| 92 | 2026-08-08T13:00Z | 5 stats | H4 (gene-space overlap) is a NULL result and is reported as such: no association between train-test gene overlap and the scFM-versus-classical difference in any tested cell. | H4 predicted the scFM advantage should GROW where the held-out dataset's gene space is poorly covered by the training corpus -- the case a pretrained model is meant to help with. Measured across 13 held-out datasets with overlap spanning 0.538 to 0.946, Spearman rho is -0.34 (p=0.25) and -0.13 (p=0.67) at budget 10 for geneformer and scgpt, and +0.02 (p=0.96) and -0.09 (p=0.78) at budget all. Every point estimate is at or below zero, i.e. if anything in the direction OPPOSITE to the hypothesis, and none is significant. | Reported as a null with the effect sizes and the n=13 limitation attached, not as an absence of analysis. The correlation rests on 13 points, so it is underpowered for a weak association and cannot exclude one; what it does show is no evidence for the predicted gradient, and the point estimates do not lean the predicted way. Emitted as T6_h4_gene_overlap.csv. |
| 93 | 2026-08-08T13:50Z | 6 report | CORRECTION to the H1 section of the report: the claim covering budgets 5-100 was computed from budgets 10-100 only, silently dropping the budget=5 cell. | The sentence stated deltas spanning -0.017 to +0.001 with all CIs crossing zero and all FDR p > 0.6. Checked against T7: at budget=5 scGPT is delta -0.0594 with a bootstrap CI of [-0.1154, -0.0104] that does NOT cross zero, and FDR p 0.1496, so every one of the three quoted figures was false for the range the sentence claimed to cover. Caught by reading the summary table back rather than trusting the numbers I had written. | Rewritten from the table: across all ten restricted-budget contrasts none is significant at FDR 0.05 (p 0.150-1.000), point estimates run -0.059 to +0.001, and the scGPT budget=5 cell is called out explicitly as a CI that excludes zero while the pre-specified Wilcoxon test does not reach significance -- reported as a suggestive but unconfirmed disadvantage rather than smoothed into a flat null. The narrower 10-100 figures are retained but labelled as the subset they describe. |
| 94 | 2026-08-08T17:42Z | 5 stats | The reconstruction guard on the per-class rescoring pass FIRED on 1,966 runs and the contaminated output was discarded rather than used. Cause: the pass ignored the inner-CV-selected PCA dimensionality. | HVG+PCA's PC count is selected per run by inner CV (30, 50 or 100 observed), while every other representation is fixed-width (Geneformer 768, scGPT 512, scVI 30, Harmony 100). The rescoring used the full stored 100-column matrix regardless, so for hvg_pca it refit a HIGHER-DIMENSIONAL model than the run it claimed to reconstruct. The signature was unambiguous: 100 percent of failures were hvg_pca, 1,942 of 1,966 at budget=10 where the fewest PCs are selected, median error -0.065 macro-F1 with 94 percent of errors exceeding 0.01. | This is the guard doing exactly the job it was written for. Without it, 1,966 runs of per-class scores for a model the study never ran would have entered the rarity analysis, systematically biased AGAINST the baseline -- the direction that flatters the study's expected conclusion. Fixed by truncating to n_dims_selected before standardisation, with a raise if the selected width exceeds the stored matrix. Verified on the exact cell that previously failed 100 percent: 12 runs, 1,176 class-rows, zero failures. The contaminated T5 was deleted, not repaired. |
| 95 | 2026-08-08T21:47Z | 5 stats | H3 (rarity) resolved: rarity does not change the direction of any result, and under dataset shift the scFM penalty is LARGEST for the rarest testable classes. Rarity strata were pre-specified by prevalence, not chosen after seeing where an effect appears. | The common defence of scFM embeddings is that they earn their keep on rare populations. Measured: in distribution at unrestricted labels the gain is present in every stratum (Geneformer +0.035 rare, +0.057 uncommon, +0.037 common); under dataset shift the penalty is present in every stratum and reaches -0.168 (Geneformer) and -0.175 (scGPT) in the 1-5 percent stratum, both FDR p < 0.005. Tested continuously rather than binned, no trend with prevalence in 11 of 12 cells (|rho| <= 0.144, p > 0.15); the single exception is uncorrected for 12 tests and is not claimed as a gradient. | Reported as a directional null with the boundary stated: the <1 percent stratum held only 12 (dataset, cell type) instances corpus-wide and was NOT tested, so conclusions about rarity extend to roughly 1 percent prevalence and no further. That is the one regime where the rare-population defence could still survive, and the report says so rather than implying the question is closed. Emitted as T8_h3_rarity.csv. |
| 96 | 2026-08-08T21:48Z | 5 stats | Mechanism evidence for the S3 reversal added as an explicitly DESCRIPTIVE analysis: both scFM embeddings carry positive dataset silhouette while all three classical representations carry negative, and dataset silhouette orders perfectly with the transfer penalty across the five methods. | The earlier report proposed dataset-of-origin encoding as the explanation for the reversal but had measured nothing. An assertion of mechanism with no measurement behind it is the kind of claim a reader cannot check. Silhouette width computed twice on an identical fixed subsample -- once by cell type, once by dataset -- gives Geneformer +0.017 versus +0.047, i.e. it separates datasets MORE strongly than cell types. | Labelled descriptive in both the results section and the limitations: n=5 representations are not independent replicates, the metric uses a fixed subsample, and correlation across five methods cannot establish causation. It motivates two named next experiments rather than concluding this one. Emitted as T9_embedding_structure.csv. |
| 97 | 2026-08-08T21:50Z | 6 report | The Spearman correlation in the mechanism analysis is reported with an EXACT permutation p (0.017), not scipy's default asymptotic p (which printed as 0.000000). | At rho = +1.00 with n = 5 the t-approximation scipy uses by default diverges and reports a p-value of essentially zero. That would have been a badly misleading number on a five-point correlation. The exact two-sided permutation p is 2/120 = 0.0167, and 0.0167 is the SMALLEST p attainable at n=5 regardless of the data. | Reported as the exact value with n stated on the figure and in the text. Recorded because the failure mode is silent: the asymptotic p is what any default call returns, and it looks like an extremely strong result rather than the floor of what five points can express. |
| 98 | 2026-08-08T21:53Z | 6 report | PORTABILITY DEFECT fixed before the fresh-clone reproducibility check: the two operational scripts hardcoded this machine's data-volume path. | fit_scvi_isolated.sh and supervise_scvi.sh are the entry points a fresh clone would run, and both hardcoded the volume path, the conda prefix and the environment path. The reproducibility check would have failed on any other machine, and the failure would have looked like a code defect rather than a configuration assumption. Found by the pre-push confidentiality scan, which flags volume paths for a different reason. | Locations now come from SCFM_DATA_ROOT, SCFM_CONDA_PREFIX and SCFM_ENV, with defaults reproducing this host exactly so nothing changes here. Verified: both scripts parse, the defaults resolve to a conda.sh and an environment that exist, and an override to a different root propagates correctly. |
| 99 | 2026-08-09T02:28Z | 7 release | Fresh-clone reproducibility check run against the REMOTE, not the working tree. Passed on every substantive dimension; three apparent failures were defects in the CHECK SCRIPT, and one of them exposed a real repository defect. | Cloning the working tree would have proved nothing about what a reader receives. Results from the clone: 104 files, 25 modules all import, 29/29 tests pass, every stage CLI parses --help, the config follows SCFM_DATA_ROOT to a different root, and all seven headline numbers in the report reproduce from the committed tables. The three flags were mine: the config entry point is config.load not load_config; my grep for hardcoded paths used grep -l so it printed filenames whose ONLY matches were the documented := default lines (0 non-default references, i.e. the scripts are portable); and the first stage is s01_data, not s01_qc. | The check script was corrected and re-run rather than its output taken at face value. Recorded because a check that reports failures it invented is worse than no check: it trains the reader to discount real ones. |
| 100 | 2026-08-09T02:30Z | 7 release | REAL DEFECT found by the fresh-clone check: src/scfmbench/stages/s04_embed.py was a 27-line scaffold whose body raised NotImplementedError, and the README's design-commitments table cited it as the enforcement point for BOTH the train-only leakage guarantee and the zero-shot-only guarantee. | It was scaffolded early to fix the pipeline shape and was superseded by s04_embed_geneformer.py, s04b_embed_scgpt.py and s04c_classical.py. A reader auditing the study's central claim would have opened a file that does nothing. Shipping a stub that a guarantee table points at is worse than shipping no pointer at all -- it converts a verifiable claim into an unverifiable one while looking rigorous. | Stub deleted; the table now points at code that exists and does the work: classical.py's fit functions take an explicit train_mask with _check_train_mask raising LeakageError on an all-True mask, the embedding stages run under torch.no_grad() with no optimiser constructed, and assert_calibration_untouched is cited for the reserved partition. My FIRST correction was itself wrong -- I invented assert_train_only, which does not exist -- so a check was added that every backticked reference in the table resolves to a real file or symbol: 17 refs, 0 unresolved. |
| 101 | 2026-08-09T02:35Z | 7 release | The report PDF is rendered with reportlab rather than pandoc or a LaTeX toolchain, because neither is installed and weasyprint fails on a missing system library (libpango). | The deliverable is a rendered PDF, not a specific toolchain. Installing a LaTeX distribution or the pango stack to satisfy a formatting step would have been a large dependency addition at release time for no scientific gain, against the brief's instruction to time-box dependency fights and take a documented fallback. reportlab was already present and needs no system libraries. | Verified by extracting the text back out of the rendered file: no literal markdown markers survive, no undefined glyphs, Limitations still precedes Results, and the H3, mechanism and decision-rule sections are all present. Two rendering defects were caught by LOOKING at the output rather than trusting the exit code -- bold spanning a line break rendered as literal asterisks, and a superscript rendered as a box -- both caused by formatting line-by-line instead of joining paragraphs first, and both fixed. |
